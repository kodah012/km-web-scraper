using System;
using System.Threading.Tasks;
using KMWebScraper.Navigation;

namespace KMWebScraper
{
    public class Scratchpad
    {
        /*
        private Browser browser = new Browser()
        {
            WebsiteUri = new Uri("https://www.google.com"),
            UserAgent = "user agent"
        };

        public async Task TestMethod()
        {
            await browser.Navigate(new Uri("")); // User agent should already be populated

            browser.UserAgent = "new user agent";

            await browser.Navigate(new Uri("second navigate w/ new UserAgent"));
        }*/
    }
    
}